import 'package:flutter/material.dart';
import 'package:salon_flutter/feature/createaccountscreen/body.dart';

class CreateAccountScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
