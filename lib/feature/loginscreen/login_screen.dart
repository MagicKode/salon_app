import 'package:flutter/material.dart';
import 'package:salon_flutter/feature/loginscreen/body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}