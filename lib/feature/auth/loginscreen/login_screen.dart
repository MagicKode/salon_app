import 'package:flutter/material.dart';
import 'package:salon_flutter/feature/auth/loginscreen/body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginBody(),
    );
  }
}